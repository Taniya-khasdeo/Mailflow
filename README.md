# Mailflow
An email scheduler lets users set a future time to send emails. The backend stores the email as Scheduled and automatically sends it at the chosen time. After delivery, the status updates to Sent or Failed.
